import {
  DATABASE_NAME,
  DATABASE_USER,
  DATABASE_PASSWORD,
  DATABASE_PORT,
  DATABASE_HOST,
} from './environment';

export const dbName = DATABASE_NAME;
export const dbUser = DATABASE_USER;
export const dbPassword = DATABASE_PASSWORD;
export const dbPort = DATABASE_PORT;
export const dbHost = DATABASE_HOST;
